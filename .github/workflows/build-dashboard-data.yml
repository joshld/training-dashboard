name: Build dashboard data

on:
  pull_request:
    paths:
      - 'activities/**/*.md'
      - 'plans/**/*.md'
      - 'knowledge/**/*.md'
      - 'scripts/build-dashboard-data.mjs'
      - 'scripts/build-workout-library.mjs'
      - 'scripts/build-nutrition-guidance.mjs'
      - '.github/workflows/build-dashboard-data.yml'
  push:
    branches: [main]
    paths:
      - 'activities/**/*.md'
      - 'plans/**/*.md'
      - 'knowledge/**/*.md'
      - 'scripts/build-dashboard-data.mjs'
      - 'scripts/build-workout-library.mjs'
      - 'scripts/build-nutrition-guidance.mjs'
      - '.github/workflows/build-dashboard-data.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Generate sanitised dashboard data
        run: node scripts/build-dashboard-data.mjs

      - name: Generate workout library
        run: node scripts/build-workout-library.mjs

      - name: Generate nutrition guidance
        run: node scripts/build-nutrition-guidance.mjs

      - name: Validate generated JSON
        run: |
          node -e "JSON.parse(require('node:fs').readFileSync('docs/generated-data.json','utf8')); console.log('generated-data.json is valid')"
          node -e "JSON.parse(require('node:fs').readFileSync('docs/workout-library.json','utf8')); console.log('workout-library.json is valid')"
          node -e "JSON.parse(require('node:fs').readFileSync('docs/nutrition-guidance.json','utf8')); console.log('nutrition-guidance.json is valid')"

      - name: Commit generated data
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        run: |
          if git diff --quiet -- docs/generated-data.json docs/workout-library.json docs/nutrition-guidance.json; then
            echo 'Generated data is already current.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/generated-data.json docs/workout-library.json docs/nutrition-guidance.json
          git commit -m 'Generate dashboard, workout and nutrition data'
          git push
