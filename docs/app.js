const pageMeta={dashboard:['Dashboard','Your current training, recovery and upcoming work.'],plan:['Training Plan','Your schedule across the next training weeks.'],activities:['Activities','Completed running, strength and other sessions.'],performance:['Performance','Training volume and progression indicators.'],recovery:['Recovery','Current recovery and training-readiness notes.'],nutrition:['Nutrition','Fueling, hydration and recovery priorities for current training.'],coach:['Coach','Current observations, priorities and recommendations.']};
const IMPORT_STORAGE_KEY='trainingLogImportedActivities';
let dashboardData=null;let charts=[];
const escapeHtml=(value='')=>String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const byId=id=>document.getElementById(id);const setText=(id,value)=>{const el=byId(id);if(el)el.textContent=value;};const setHtml=(id,value)=>{const el=byId(id);if(el)el.innerHTML=value;};
function loadImportedActivities(){try{const value=JSON.parse(localStorage.getItem(IMPORT_STORAGE_KEY)||'[]');return Array.isArray(value)?value:[];}catch(error){console.warn('Imported activities could not be loaded',error);return[];}}
function fmtDuration(seconds){if(!Number.isFinite(seconds))return null;const h=Math.floor(seconds/3600),m=Math.floor((seconds%3600)/60),s=Math.round(seconds%60);return h?`${h}h ${m}m`:`${m}:${String(s).padStart(2,'0')}`;}
function importedToSession(item){const date=item.date?item.date.slice(0,10):'Imported activity';const parts=[];if(item.distanceKm!=null)parts.push(`${Number(item.distanceKm).toFixed(2)} km`);if(item.durationSeconds!=null)parts.push(fmtDuration(Number(item.durationSeconds)));if(item.averagePace&&item.averagePace!=='—')parts.push(item.averagePace);if(item.averageHeartRate!=null)parts.push(`${item.averageHeartRate} bpm avg`);return{date,activity:item.activity||'Other',summary:parts.join(' · ')||`${item.activityType||'Activity'} imported from ${item.sourceFormat||'file'}`,rpe:item.rpe||'—',imported:true};}
function mergeSessions(base=[]){const imported=loadImportedActivities().map(importedToSession);return [...imported,...base].sort((a,b)=>Date.parse(b.date||0)-Date.parse(a.date||0));}
function renderDocument(targetId,documentData){const target=byId(targetId);if(!target||!documentData)return;target.innerHTML=`<div class="document-header"><div><p class="eyebrow">Training Log</p><h2>${escapeHtml(documentData.title)}</h2><p>${escapeHtml(documentData.intro)}</p></div></div><div class="document-grid">${documentData.sections.map(section=>`<section class="document-section"><h3>${escapeHtml(section.heading)}</h3><ul>${section.items.map(item=>`<li>${escapeHtml(item)}</li>`).join('')}</ul></section>`).join('')}</div>`;}
function openTab(name){const buttons=[...document.querySelectorAll('.nav-item')],selected=buttons.find(button=>button.dataset.tab===name)||buttons[0];buttons.forEach(item=>item.classList.toggle('active',item===selected));document.querySelectorAll('.tab-panel').forEach(panel=>panel.classList.toggle('active',panel.id===`tab-${selected.dataset.tab}`));const meta=pageMeta[selected.dataset.tab]||pageMeta.dashboard;setText('pageTitle',meta[0]);setText('pageSubtitle',meta[1]);history.replaceState(null,'',`#${selected.dataset.tab}`);window.scrollTo({top:0,behavior:'smooth'});charts.forEach(chart=>chart.resize());}
function setupNavigation(){document.querySelectorAll('.nav-item').forEach(button=>button.addEventListener('click',()=>openTab(button.dataset.tab)));document.querySelectorAll('[data-open-tab]').forEach(button=>button.addEventListener('click',()=>openTab(button.dataset.openTab)));openTab(location.hash.replace('#','')||'dashboard');}
function workoutTypeClass(title=''){const value=title.toLowerCase();if(value.includes('easy'))return'easy';if(value.includes('long')||value.includes('race'))return'long';if(value.includes('tempo'))return'tempo';if(value.includes('interval'))return'interval';if(value.includes('strength')||value.includes('arms')||value.includes('push')||value.includes('pull'))return'strength';return'general';}
function workoutIcon(title=''){const value=title.toLowerCase();if(value.includes('easy'))return'↗';if(value.includes('long')||value.includes('race'))return'◒';if(value.includes('tempo'))return'≈';if(value.includes('interval'))return'⚡';if(value.includes('strength')||value.includes('arms')||value.includes('push')||value.includes('pull'))return'◆';return'•';}
function renderCalendar(plan){const container=byId('calendarWeeks');if(!container)return;container.innerHTML=plan.weeks.map(week=>`<article class="calendar-week ${week.current?'current-week':''}"><div class="calendar-week-head"><div><p class="eyebrow">${escapeHtml(week.dateRange)}</p><h3>${escapeHtml(week.name)}</h3></div><div><strong>${escapeHtml(week.plannedDistance)}</strong><small>planned</small></div></div><div class="calendar-grid">${['Mon','Tue','Wed','Thu','Fri','Sat','Sun'].map(day=>{const workout=week.workouts.find(item=>item.day===day);return `<div class="calendar-day"><span class="day-name">${day}</span>${workout?`<div class="calendar-workout ${workoutTypeClass(workout.title)} ${workout.completed?'complete':''}"><small>${workout.completed?'Completed':escapeHtml(workout.distance)}</small><strong>${escapeHtml(workout.title)}</strong></div>`:'<div class="calendar-rest">Rest</div>'}</div>`;}).join('')}</div></article>`).join('');}
function renderPlan(plan){const container=byId('planWeeks');if(!container)return;container.innerHTML=plan.weeks.map(week=>{const completed=week.workouts.filter(workout=>workout.completed).length,percent=Math.round(completed/week.workouts.length*100);return `<article class="panel plan-week ${week.current?'current-week':''}"><div class="week-card-header"><div><p class="eyebrow">${escapeHtml(week.dateRange)}</p><h2>${escapeHtml(week.name)}</h2><p class="week-caption">${completed} of ${week.workouts.length} sessions complete</p></div><div class="week-total"><strong>${escapeHtml(week.plannedDistance)}</strong><small>planned</small></div></div><div class="progress-track"><div class="progress-fill" style="width:${percent}%"></div></div><div class="week-meta"><span>${percent}% complete</span><span>${escapeHtml(week.progressDistance)}</span></div><div class="workout-list compact-workouts">${week.workouts.map((workout,index)=>{const details=Boolean(workout.summary||(workout.steps&&workout.steps.length));return `<details class="workout-card ${workoutTypeClass(workout.title)} ${workout.completed?'complete':''}" ${index===week.workouts.length-1&&week.current?'open':''}><summary><span class="workout-day">${escapeHtml(workout.day)}</span><span class="workout-type-icon">${workout.completed?'✓':workoutIcon(workout.title)}</span><span class="workout-card-title"><strong>${escapeHtml(workout.title)}</strong><small>${escapeHtml(workout.distance)}${workout.completed?' · Completed':''}</small></span>${details?'<span class="workout-chevron">⌄</span>':''}</summary>${details?`<div class="workout-card-detail">${workout.summary?`<p>${escapeHtml(workout.summary)}</p>`:''}${workout.steps?`<ol>${workout.steps.map(step=>`<li>${escapeHtml(step)}</li>`).join('')}</ol>`:''}</div>`:''}</details>`;}).join('')}</div></article>`;}).join('');}
function renderActivities(sessions,filter='all'){const filtered=filter==='all'?sessions:sessions.filter(item=>item.activity===filter);const html=filtered.map(item=>{const klass=String(item.activity||'other').toLowerCase();const icon=item.activity==='Running'?'R':item.activity==='Strength'?'S':'•';return `<article class="activity-card"><div class="activity-icon ${klass}">${icon}</div><div><p class="activity-date">${escapeHtml(item.date)}${item.imported?' · Imported':''}</p><h3>${escapeHtml(item.activity)}</h3><p>${escapeHtml(item.summary)}</p></div><span class="pill">RPE ${escapeHtml(item.rpe||'—')}</span></article>`;}).join('');setHtml('activityCards',html||'<p class="empty-state">No matching activities.</p>');setHtml('dashboardActivities',html||'<p class="empty-state">No activities logged yet.</p>');}
function renderWorkoutReport(){setText('reportTitle','Over/under tempo');setText('reportScore','9.5 / 10');setHtml('reportSummary',`<p class="report-lead">All four quality kilometres were completed within three seconds of their prescribed pace.</p><div class="report-callout"><strong>Coach takeaway</strong><p>Excellent pacing discipline. The faster reps stayed controlled, and the session felt comfortably hard rather than maximal.</p></div>`);const reps=[['4:35','Target 4:35','On target'],['4:18','Target 4:15','+3 sec'],['4:33','Target 4:35','−2 sec'],['4:18','Target 4:15','+3 sec']];setHtml('reportReps',reps.map((rep,index)=>`<div class="rep-card"><span>Rep ${index+1}</span><strong>${rep[0]}</strong><small>${rep[1]}</small><em>${rep[2]}</em></div>`).join(''));}
function setupFilters(){document.querySelectorAll('.filter').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(item=>item.classList.toggle('active',item===button));if(dashboardData)renderActivities(dashboardData.sessions,button.dataset.filter);}));}
function createDistanceChart(id,data){const ctx=byId(id);if(!ctx)return;const fallback=byId(`${id}Fallback`);if(typeof Chart==='undefined'){ctx.hidden=true;if(fallback)fallback.hidden=false;return;}try{const chart=new Chart(ctx,{type:'line',data:{labels:data.labels,datasets:[{label:'Distance (km)',data:data.values,borderColor:'#71e6b3',backgroundColor:'rgba(113,230,179,.12)',fill:true,tension:.34,spanGaps:true,pointRadius:4,pointHoverRadius:6}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{x:{ticks:{color:'#8e9ab0'},grid:{color:'rgba(43,54,80,.45)'}},y:{beginAtZero:true,ticks:{color:'#8e9ab0'},grid:{color:'rgba(43,54,80,.45)'}}}}});charts.push(chart);}catch(error){console.error(`Chart ${id} failed`,error);ctx.hidden=true;if(fallback)fallback.hidden=false;}}
async function loadNutrition(){try{const response=await fetch(`nutrition.json?v=${Date.now()}`,{cache:'no-store'});if(!response.ok)throw new Error(`HTTP ${response.status}`);renderDocument('nutritionContent',await response.json());}catch(error){console.error('Nutrition load failed',error);setHtml('nutritionContent','<div class="notice">Nutrition guidance could not be loaded. Please refresh after deployment completes.</div>');}}
function renderCore(data){setText('updated',`Last updated ${data.updated}`);setText('status',data.status);setHtml('metrics',data.metrics.map(item=>`<article class="metric"><span>${escapeHtml(item.label)}</span><strong>${escapeHtml(item.value)}</strong><small>${escapeHtml(item.note||'')}</small></article>`).join(''));setHtml('coachFocus',data.coachFocus.map(item=>`<li>${escapeHtml(item)}</li>`).join(''));setHtml('strengthRows',data.strength.map(row=>`<tr><td>${escapeHtml(row.exercise)}</td><td>${escapeHtml(row.load)}</td><td>${escapeHtml(row.work)}</td></tr>`).join(''));setText('latestTitle',data.latestSession.title);setText('latestType',data.latestSession.type);setHtml('latestDetails',data.latestSession.details.map(item=>`<div class="detail"><span>${escapeHtml(item.label)}</span><strong>${escapeHtml(item.value)}</strong></div>`).join(''));const next=data.plan.nextWorkout;setText('nextWorkoutTitle',next.title);setText('nextWorkoutDate',next.date);setHtml('nextWorkoutDetails',next.details.map(item=>`<div class="detail"><span>${escapeHtml(item.label)}</span><strong>${escapeHtml(item.value)}</strong></div>`).join(''));const current=data.plan.weeks.find(week=>week.current)||data.plan.weeks[0],completed=current.workouts.filter(workout=>workout.completed).length,percent=Math.round(completed/current.workouts.length*100);setText('currentWeekTitle',`${current.name} · ${current.dateRange}`);setText('currentWeekProgressText',`${completed}/${current.workouts.length}`);const progress=byId('currentWeekProgress');if(progress)progress.style.width=`${percent}%`;setHtml('currentWeekSummary',`<span>${escapeHtml(current.progressDistance)}</span><span>${escapeHtml(current.plannedDistance)} planned</span>`);renderCalendar(data.plan);renderPlan(data.plan);renderActivities(data.sessions);renderWorkoutReport();renderDocument('runningContent',data.tabs.running);renderDocument('strengthContent',data.tabs.strength);renderDocument('recoveryContent',data.tabs.recovery);renderDocument('coachContent',data.tabs.coach);}
async function loadDashboard(){try{const response=await fetch(`data.json?v=${Date.now()}`,{cache:'no-store'});if(!response.ok)throw new Error(`HTTP ${response.status}`);const data=await response.json();data.sessions=mergeSessions(data.sessions||[]);dashboardData=data;renderCore(data);createDistanceChart('distanceChart',data.weeklyDistance);createDistanceChart('performanceDistanceChart',data.weeklyDistance);}catch(error){console.error('Dashboard load failed',error);setText('updated','Dashboard data could not be loaded.');setText('status','Data error');const notice=byId('dashboardNotice');if(notice){notice.hidden=false;notice.textContent='The dashboard could not load its data. Please refresh after the latest deployment completes.';}}}
document.addEventListener('DOMContentLoaded',()=>{setupNavigation();setupFilters();loadDashboard();loadNutrition();});
